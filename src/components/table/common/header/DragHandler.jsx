import React from 'react'
import { SortableHandle } from 'react-sortable-hoc'

// TODO :: add icon
const DragHandler = SortableHandle(() => <div className="reorderIcon" />)

export default DragHandler
